/**
 * Shared tokens for the ebook cold-open sections (EbookHero, EbookImmersion).
 * Both sections must render on the exact same background for the visual seam
 * between them to stay invisible — keep BG in sync via this constant, not
 * copy-pasted hex strings.
 */
export const EBOOK_COLD_BG = "#141414";
export const EBOOK_ACCENT = "#c6db70";
